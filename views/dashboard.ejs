<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Dashboard | Agreement App</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <style>
        body {
            background: linear-gradient(120deg, #87ceeb 0%, #e0e7ff 100%);
            min-height: 100vh;
            margin: 0;
            font-family: 'Segoe UI', Arial, sans-serif;
            display: flex;
            align-items: center;
            justify-content: center;
        }
        .dashboard-container {
            background: #fff;
            padding: 48px 36px;
            border-radius: 20px;
            box-shadow: 0 12px 40px rgba(59,130,246,0.12);
            max-width: 650px;
            margin: auto;
            text-align: center;
            position: relative;
            overflow: hidden;
        }
        .dashboard-container::before {
            content: "";
            position: absolute;
            top: -60px;
            left: -60px;
            width: 180px;
            height: 180px;
            background: radial-gradient(circle, #6366f1 0%, transparent 70%);
            opacity: 0.13;
            z-index: 0;
        }
        .dashboard-container::after {
            content: "";
            position: absolute;
            bottom: -60px;
            right: -60px;
            width: 180px;
            height: 180px;
            background: radial-gradient(circle, #3b82f6 0%, transparent 70%);
            opacity: 0.13;
            z-index: 0;
        }
        .dashboard-title {
            color: #3b82f6;
            font-size: 2.2em;
            font-weight: 700;
            margin-bottom: 18px;
            letter-spacing: 1px;
            position: relative;
            z-index: 1;
        }
        .dashboard-info {
            color: #374151;
            font-size: 1.13em;
            margin-bottom: 30px;
            position: relative;
            z-index: 1;
        }
        .dashboard-links {
            margin-top: 24px;
            position: relative;
            z-index: 1;
        }
        .dashboard-link-btn {
            display: inline-block;
            margin: 0 10px 12px 10px;
            background: linear-gradient(90deg, #6366f1 0%, #3b82f6 100%);
            color: #fff;
            padding: 13px 32px;
            border: none;
            border-radius: 10px;
            font-size: 1.08em;
            font-weight: 600;
            text-decoration: none;
            box-shadow: 0 2px 12px rgba(59,130,246,0.08);
            transition: background 0.2s, transform 0.2s;
        }
        .dashboard-link-btn:hover {
            background: linear-gradient(90deg, #3b82f6 0%, #6366f1 100%);
            transform: translateY(-2px) scale(1.04);
        }
        .agreement-list {
            margin-top: 32px;
            text-align: left;
            position: relative;
            z-index: 1;
        }
        .agreement-list h2 {
            color: #6366f1;
            font-size: 1.18em;
            margin-bottom: 10px;
            font-weight: 600;
        }
        .agreement-list ul {
            list-style: none;
            padding: 0;
        }
        .agreement-list li {
            background: #f1f5f9;
            margin-bottom: 12px;
            padding: 16px 12px;
            border-radius: 8px;
            box-shadow: 0 2px 8px rgba(59,130,246,0.06);
            color: #333;
            display: flex;
            align-items: center;
            justify-content: space-between;
            font-size: 1.05rem;
        }
        .agreement-list li a {
            color: #3b82f6;
            text-decoration: none;
            font-weight: 500;
            transition: color 0.2s;
        }
        .agreement-list li a:hover {
            color: #6366f1;
            text-decoration: underline;
        }
        .agreement-actions a {
            color: #6366f1;
            text-decoration: none;
            font-weight: 500;
            margin-left: 12px;
            transition: text-decoration 0.2s, color 0.2s;
        }
        .agreement-actions a:hover {
            text-decoration: underline;
            color: #3b82f6;
        }
        .agreement-actions button[type="submit"] {
            background: linear-gradient(90deg, #28a745 0%, #34d399 100%);
            color: #fff;
            border: none;
            border-radius: 6px;
            padding: 7px 18px;
            cursor: pointer;
            font-weight: 600;
            margin-right: 6px;
            transition: background 0.2s, transform 0.2s;
        }
        .agreement-actions button[type="submit"]:hover {
            background: linear-gradient(90deg, #34d399 0%, #28a745 100%);
            transform: translateY(-2px) scale(1.04);
        }
        .agreement-actions .deny-btn {
            background: linear-gradient(90deg, #dc3545 0%, #f87171 100%);
        }
        .agreement-actions .deny-btn:hover {
            background: linear-gradient(90deg, #f87171 0%, #dc3545 100%);
        }
        @media (max-width: 700px) {
            .dashboard-container {
                max-width: 98vw;
                padding: 18px 8px;
            }
            .dashboard-title {
                font-size: 1.5em;
            }
            .dashboard-link-btn {
                padding: 11px 18px;
                font-size: 1em;
            }
            .agreement-list li {
                font-size: 0.98rem;
                flex-direction: column;
                align-items: flex-start;
            }
            .agreement-actions {
                margin-top: 8px;
            }
        }
    </style>
</head>
<body>
    <div class="dashboard-container">
        <div class="dashboard-title">Welcome, <%= user.name %>!</div>
        <div class="dashboard-info">
            Phone: <strong><%= user.phone %></strong><br>
            User ID: <strong><%= user.id %></strong>
        </div>
        <div class="dashboard-links">
            <a href="/agreements/create" class="dashboard-link-btn">Create Agreements</a>
            <a href="/users/<%= user.id %>/edit" class="dashboard-link-btn">Edit Profile</a>
            <a href="/users/<%= user.id %>/delete" class="dashboard-link-btn" onclick="return confirm('Are you sure you want to delete your account?')">Delete Profile</a>
            <a href="/logout" class="dashboard-link-btn">Logout</a>
        </div>
        <div class="agreement-list">
            <h2>Agreements You Created</h2>
            <ul>
                <% if (createdAgreements && createdAgreements.length > 0) { %>
                    <% createdAgreements.forEach(agreement => { %>
                        <li>
                            <span>
                                <a href="/agreements/<%= agreement.id %>"><%= agreement.title %></a>
                                <small>(Status: <%= agreement.status %>)</small>
                            </span>
                            <span class="agreement-actions">
                                <a href="/agreements/<%= agreement.id %>/edit">Edit</a>
                                <a href="/agreements/<%= agreement.id %>/delete" onclick="return confirm('Are you sure?')">Delete</a>
                            </span>
                        </li>
                    <% }) %>
                <% } else { %>
                    <li>No agreements created by you.</li>
                <% } %>
            </ul>
            <h2>Agreements Where You Are Tagged</h2>
            <ul>
                <% if (taggedAgreements && taggedAgreements.length > 0) { %>
                    <% taggedAgreements.forEach(agreement => { %>
                        <li>
                            <span>
                                <a href="/agreements/<%= agreement.id %>"><%= agreement.title %></a>
                                <small>(Status: <%= agreement.status %>)</small>
                            </span>
                            <% if (agreement.status === 'pending') { %>
                                <span class="agreement-actions">
                                    <form action="/agreements/<%= agreement.id %>/confirm" method="POST" style="display:inline;">
                                        <button type="submit">Confirm</button>
                                    </form>
                                    <form action="/agreements/<%= agreement.id %>/deny" method="POST" style="display:inline;">
                                        <button type="submit" class="deny-btn">Deny</button>
                                    </form>
                                </span>
                            <% } else { %>
                                <span class="agreement-actions">
                                    <strong style="color:<%= agreement.status === 'confirmed' ? '#28a745' : '#dc3545' %>;">
                                        <%= agreement.status.charAt(0).toUpperCase() + agreement.status.slice(1) %>
                                    </strong>
                                </span>
                            <% } %>
                        </li>
                    <% }) %>
                <% } else { %>
                    <li>No agreements where you are tagged.</li>
                <% } %>
            </ul>
        </div>
    </div>
</body>
</html>