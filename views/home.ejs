<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Agreement App | Home</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="../public/style.css">
    <style>
        body {
            background:  #87ceeb;
            min-height: 100vh;
            margin: 0;
            font-family: 'Segoe UI', Arial, sans-serif;
            display: flex;
            align-items: center;
            justify-content: center;
        }
        .home-content {
            padding: 48px 36px;
            border-radius: 20px;
            background: rgba(255,255,255,0.95);
            box-shadow: 0 12px 40px rgba(59,130,246,0.12);
            max-width: 440px;
            margin: auto;
            text-align: center;
            position: relative;
            overflow: hidden;
        }
        .home-content::before {
            content: "";
            position: absolute;
            top: -60px;
            left: -60px;
            width: 180px;
            height: 180px;
            background: radial-gradient(circle, #6366f1 0%, transparent 70%);
            opacity: 0.13;
            z-index: 0;
        }
        .home-content::after {
            content: "";
            position: absolute;
            bottom: -60px;
            right: -60px;
            width: 180px;
            height: 180px;
            background: radial-gradient(circle, #3b82f6 0%, transparent 70%);
            opacity: 0.13;
            z-index: 0;
        }
        .logo {
            width: 80px;
            margin-bottom: 22px;
            filter: drop-shadow(0 2px 8px rgba(59,130,246,0.10));
        }
        .home-title {
            color: #3b82f6;
            font-size: 2.4em;
            font-weight: 700;
            margin-bottom: 16px;
            letter-spacing: 1px;
            position: relative;
            z-index: 1;
        }
        .home-desc {
            color: #374151;
            font-size: 1.18em;
            margin-bottom: 32px;
            line-height: 1.6;
            position: relative;
            z-index: 1;
        }
        .home-links {
            margin-top: 24px;
            position: relative;
            z-index: 1;
        }
        .home-link-btn {
            display: inline-block;
            margin: 0 10px 12px 10px;
            background: linear-gradient(90deg, #6366f1 0%, #3b82f6 100%);
            color: #fff;
            padding: 13px 32px;
            border: none;
            border-radius: 10px;
            font-size: 1.08em;
            font-weight: 600;
            text-decoration: none;
            box-shadow: 0 2px 12px rgba(59,130,246,0.08);
            transition: background 0.2s, transform 0.2s;
        }
        .home-link-btn:hover {
            background: linear-gradient(90deg, #3b82f6 0%, #6366f1 100%);
            transform: translateY(-2px) scale(1.04);
        }
        @media (max-width: 600px) {
            .home-content {
                padding: 24px 8px;
                max-width: 98vw;
            }
            .logo {
                width: 60px;
            }
            .home-title {
                font-size: 1.5em;
            }
            .home-link-btn {
                padding: 11px 18px;
                font-size: 1em;
            }
        }
    </style>
</head>
<body>
    <div class="home-content">
        <img src="https://img.icons8.com/fluency/96/contract.png" alt="Agreement Logo" class="logo"/>
        <div class="home-title">Welcome to Agreement App</div>
        <div class="home-desc">
            Easily create, manage, and confirm agreements between parties.<br>
            <span style="color:#6366f1;font-weight:500;">Secure, simple, and fast.</span>
        </div>
        <div class="home-links">
            <a href="/login" class="home-link-btn">Login</a>
            <a href="/register" class="home-link-btn">Register</a>
            <a href="/agreements" class="home-link-btn">View Agreements</a>
        </div>

    </div>
</body>
</html>